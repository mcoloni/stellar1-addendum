json.extract! telescope, :id, :brand, :name, :description, :notes, :angular_resolution, :magnitude, :focal_ratio, :finderscope, :focal_length, :mount, :aperture, :typology, :created_at, :updated_at
json.url telescope_url(telescope, format: :json)
